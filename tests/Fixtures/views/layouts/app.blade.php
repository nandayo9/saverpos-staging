{{-- Test-only stand-in for the Ultimate POS layout. Prepended to the view
     finder so a Recommerce screen can be rendered without booting the whole
     POS chrome, while still exercising the real Blade of the screen itself. --}}
@yield('content')
